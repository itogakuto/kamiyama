{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "illustration": "node scripts/build-illustration-map.js",
    "geocode": "node scripts/geocode-addresses.js",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "wrangler pages deploy dist --project-name kamiyama-map"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "wrangler": "latest"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
